import { useScreenSize, ScreenSizeProvider } from './ScreenSize'

export { useScreenSize, ScreenSizeProvider }

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  useScreenSize,
  ScreenSizeProvider,
}
